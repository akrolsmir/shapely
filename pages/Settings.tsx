import BasePage from '../components/base-page'

export default function Settings(props: {}) {
  return (
    <BasePage>
      <p>
        TODO: settings: age range preferences, gender pref, location, max
        distance
      </p>
      <p>
        note, hinge has (we could model as dealbreakers): ethnicity, religion,
        height, has children, family plans, education level, politics, drinking,
        smoking, marajuana, drugs
      </p>
      <button> Take a break </button>
    </BasePage>
  )
}
